<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.txstate.gameapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MenuActivity" />
        <activity android:name=".RpsActivity" />
        <activity android:name=".ButtonMashActivity" />
        <activity android:name=".PongActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".OnePlayer" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TwoPlayer" />
        <activity android:name=".NumberPlayers" />
        <activity android:name=".TwoPlayerRpsActivity"></activity>
    </application>

</manifest>